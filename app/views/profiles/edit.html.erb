<!DOCTYPE html>
<html>
<head>
    <style>
        .profile-container {
            text-align: center;
            background-color: #f1f1f1;
        }

        .profile-title {
            font-size: 36px;
            font-weight: bold;
            color: #333;
        }

        .item {
            display: inline-block;
            margin: 5px;
            padding: 10px;
            background-color: #ccc;
        }

        .buttons {
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="profile-title">
            User Profile
        </div>
        <img alt="Profile Picture">
        <%= form_with url: profile_path, multipart: true do |form| %>
            <%= file_field_tag :picture %>
            <%= form.submit 'Upload Icon'%>
        <% end %>
        <!-- Form for changing the user's name -->
        <%= form_with model: @user, url: profile_path, method: :patch do |form| %>
            <%= form.label :name, "Change Name" %>
            <%= form.text_field :name %>
            <%= form.submit 'Update Name' %>
        <% end %>
        <div class="buttons">
            <a href="<%= profile_path %>" id="cancel-link">Cancel</a>
        </div>
    </div>
</body>
</html>
